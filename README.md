This service is created for generate PDfs
there is no ENV file or dependency at the moment

npm run dev for development envirement


cd /path/to/my-app/pdf-service
git init
git add .
git commit -m "Initial commit for pdf-service"

gh repo create pdf-service --private --source=. --remote=origin --push